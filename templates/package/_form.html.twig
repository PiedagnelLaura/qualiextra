{{ form_start(form) }}

<div class="container my-4">

    {{ form_widget(form) }}
    <div class='btn-save'>
        <button class="btn button from-center">{{ button_label|default('Sauvegarder') }}</button>
    </div>
</div>


{{ form_end(form) }}
