<header>

			 <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                <a href="{{path("app_user_home")}}" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none header-logo">
                    <img src="{{asset('asset/logo/logo.svg')}}" alt="" width="30" height="24" class="logo">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{path("app_user_home")}}">Mes expériences</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" href="{{path("app_user_resto_address")}}">Les bonnes adresses</a>
                        </li>
                        
                    </ul>
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                        
                           
                
                            
                            {% if app.user == null %}
                                <a href="{{ path('app_login')}}" class="btn btn-danger me-2">Me Connecter</a>
                            {% else %}
                                <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle active" href="#" id="navbarDropdown" role="button"
                                    data-bs-toggle="dropdown">
                                    <span>Bonjour {{app.user.firstname}} {{app.user.lastname}}</span>
                                    <i class="bi bi-person-square"></i>
                                </a>
                                
                                <ul class="dropdown-menu">
                                   
                                        
                                    </li>
                                    <li><a class="dropdown-item" href="{{path("app_logout")}}">Déconnexion</a></li>
                                </ul>
                            </li>
                            {% endif %} 
                            

                    </ul>

                   
                </div>
            </div>
        </nav>
		</header>

		{#Ajout du flash message pour le client#}

		{% for label, messages in app.flashes %}
			{% for message in messages %}
				{% set explode = label|split(' ') %}

				<div class="alert alert-{{label}} alert-dismissible fade show" role="alert">
					{{ message }}
					{% if explode[1] is defined %}
						<a href="{{path("app_user_flash",{'id':explode[1]})}}">
							<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
						</a>
					{% else %}
						<button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
					{% endif %}

				</div>

			{% endfor %}
		{% endfor %}

		{#Fin du flash message pour le client#}
