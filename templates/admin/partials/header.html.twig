<header>
        <nav class="navbar navbar-expand-lg nav-color">
            <div class="container-fluid">
              <a class="navbar-brand" href="{{path("app_admin_home")}}">
                <img src="{{asset('asset/logo/logo.svg')}}" alt="" width="30" height="24" class="logo">
              </a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon navbar-toggler-color"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle nav-lien" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">Bonjour {{app.user.firstname ?? "Mme/Mr"}} {{app.user.lastname ?? " "}} </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    {% if app.user !=null %}
                      <li><a class="dropdown-item" href="{{path("app_logout")}}">Déconnection</a></li>
                    {% else %}
                      <li><a class="dropdown-item" href="{{path("app_login")}}">Connection</a></li>
                    {% endif %} 
                    </ul>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link nav-lien" aria-current="page" href="{{path("app_admin_home")}}">Accueil</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link nav-lien" aria-current="page" href="#">Ajouter un prestataire</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link nav-lien" aria-current="page" href="#">Ajouter un établissement</a>
                  </li>
                </ul>
               
            </div>
</header>
	<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pprn3073KE6tl6bjs2QrFaJGz5/SUsLqktiwsUTF55Jfv3qYSDhgCecCxMW52nD2" crossorigin="anonymous"></script>
